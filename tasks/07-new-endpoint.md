# Task 7: Add Health Check Endpoint

Use agent mode:

```
@workspace Add a health check endpoint at /api/health that returns status, database connection, and timestamp. Follow existing patterns.
```

Verify:
```bash
curl http://localhost:8000/api/health
```
