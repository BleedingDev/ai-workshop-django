# Task 5: Fix Errors with /fix

Run `ruff check .` to see errors.

Open a file with errors, select the problematic code, then:

```
/fix
```

**Files with errors:**
- `conduit/apps/articles/views.py`
- `conduit/apps/articles/serializers.py`
- `conduit/apps/authentication/serializers.py`
